[project]
name = "cuda-learning"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.4.6",
    "torch>=2.12.0",
    "triton>=3.7.0",
]
